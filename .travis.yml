language: node_js
before_install:
  - npm install -g bower
  - npm install -g grunt-cli
  - gem install sass
install:
  - npm install
  - bower install
  - make place-assets
  - grunt build
script:
  - screen -d -m npm start
  - make test
