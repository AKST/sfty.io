/**
 * @jsx React.DOM
 */

Sfty.View.Graphizer = React.createClass({

  propTypes: {
    data: React.PropTypes.object,
  },

  render: function () {

    console.log(this.props.data);

    return (
      <div>Graph?</div>
    );
  },

});